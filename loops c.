#include <stdio.h>
int main() {
    int a = 0, b=1;
    for (int i=1; i<7; i++)
	{
        for (int j=i; j<=i && j>0; j--)
		{
            printf("01", j);
        }
        printf("\n");
    }
    return 0;
}
